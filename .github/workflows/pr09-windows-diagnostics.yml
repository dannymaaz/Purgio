name: "PR-09 Windows diagnostics hardening"

on:
  push:
    branches:
      - pr-09-windows-cleanup-safe

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Apply diagnostics hardening
        run: python3 scripts/pr09_windows_diagnostics.py
      - name: Commit product changes
        run: |
          git config user.name "Danny Maaz"
          git config user.email "61290346+dannymaaz@users.noreply.github.com"
          git add src-tauri/src/scanner.rs src/i18n.tsx
          git commit -m "Harden Windows diagnostics cleanup scope"
          git push origin HEAD:pr-09-windows-cleanup-safe
